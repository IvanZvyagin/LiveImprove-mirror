spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${SUPABASE_ISSUER_URI}
          jwk-set-uri: ${SUPABASE_ISSUER_URI}/.well-known/jwks.json
app:
  frontend-origin: ${FRONTEND_ORIGIN}