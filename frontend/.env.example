# Базовый URL API для сборки Vite (в браузере). Локально с proxy в vite.config.ts
# можно ходить на относительный путь /api/... и не задавать переменную.
# Для docker-compose.dev.yml задаётся build-arg VITE_API_URL.
VITE_API_URL=http://localhost:8080/api/v1
